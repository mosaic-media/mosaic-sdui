name: verify

on:
  push:
    branches: [main]
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"
      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      # The schema is the single source of truth: fail if the committed bindings
      # are stale (generate + git diff).
      - name: Generated bindings are current
        run: bash scripts/check-generated.sh

      # Go build + unit + schema-conformance tests.
      - name: Go test
        run: go test ./...

      # The generated TypeScript typechecks.
      - name: TypeScript typecheck
        run: npx --yes typescript@5 tsc --noEmit --strict ts/contract.gen.ts
